# workout

A bodyweight workout tracker — three beginner home workouts, no equipment.

Single static page, no build step, no accounts. Completed workouts are stored
in the browser's `localStorage`, so nothing leaves the device.

Served by GitHub Pages from `main`: https://zeesykessler-alt.github.io/workout/
